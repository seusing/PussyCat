# BLOCKED — 集成待裁决/外部项

当前:**无阻塞**。

## 常备规则备忘(非阻塞项)

- **付费调用门**:新增付费模型调用仅在仓内已有明确预算批准记录时执行。vk 仓
  HUMAN-QUEUE HQ-06 当前无未决申请、无未消费的用途内批复——因此本集成默认全程
  零新增付费调用(fixture E2E + 既有真实语料 cache/DB 复用)。如某阶段确需真实
  LLM 调用,先在此立项(用途/预估/上限)并转入 blocked-on-budget,继续机器侧其他
  工作。
- **vk 人工门(HQ-01~08)**:属上游 M 线人工事项,不由本集成代办、不代填;爪爪 UI
  按 machine_ready / partial 如实展示(拍板 8)。
- **卸载/安装真机验证**:属阶段 6;若届时需要用户在场的操作(如 MSI 安装 UAC 弹窗
  无法自动化),记录三次尝试与责任边界后在此立项。
