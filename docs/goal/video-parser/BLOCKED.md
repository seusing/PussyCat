# 待裁决清单

## 不阻断后续工作的约束冲突

- 固定语料现有 6 条；若严格执行“每条冷/热各跑 3 次”，至少需要 36 次真实模型调用（即使只按最低 5 条也需 30 次），但任务书同时把整个目标的真实模型调用总数硬限制为 15 次。这两项无法同时满足。
- 当前处理：不伪造观测，也不突破总额度。阶段 2 只使用 3 次真实调用完成明确的约 1 分钟热进程门槛；其余 12 次留给阶段 3 备用切换、阶段 4 采集分支和阶段 5/6 深度 smoke。固定语料与完整 JSONL 契约已经冻结，后续可在调用额度被明确提高时补跑全矩阵。
- 这不阻断阶段 3–7 的实现和离线/fixture 验证；最终交付必须把全语料基准标为未完成，除非后续仍能在 15 次总额度内获得所需的真实观测。
